// Package handlers contains the gin HTTP handlers — one file per resource.
// Handlers stay thin: parse request, call the service, write the response.
// Business logic and DB access live in internal/service.
package handlers
